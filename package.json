{
  "name": "orejs",
  "version": "0.0.0",
  "description": "orejs is helper library (written in Javascript) to provide simple high-level access to the ore-protocol. Orejs uses eosJS as a wrapper to the EOS blockchain",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npm run lint && npm run compile",
    "compile": "tsc --project ./tsconfig.json --pretty",
    "example": "npm run build && node --harmony example",
    "lint": "tslint  src/*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/api.market/orejs.git"
  },
  "author": "Aikon",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/api.market/orejs/issues"
  },
  "homepage": "https://github.com/api.market/orejs#readme",
  "devDependencies": {
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  },
  "dependencies": {}
}
